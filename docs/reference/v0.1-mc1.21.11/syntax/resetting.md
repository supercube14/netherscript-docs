# Resetting

Upon compilation, NetherScript inside of the `reset` codeblock is put into the `NAMESPACE:reset` function in the compiled datapack.

```ntscript
reset
    # reset scoreboards
    # clear data
    # etc ...
end
```

