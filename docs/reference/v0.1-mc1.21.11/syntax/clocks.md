# Clocks

Clocks are codeblocks whose contents are placed into a separate `.mcfunction` file (using the same name as the codeblock), and added to the `minecraft:tick` function tag.

## Defining

```ntscript
clock NAME
    ...
end
```